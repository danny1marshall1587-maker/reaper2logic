#!/bin/bash
# REAPER ↔ Logic Pro Converter macOS Launcher
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
echo "Launching REAPER ↔ Logic Pro Converter..."
open "$DIR/index.html"
